registerListener('load', () => setTimeout(removePreload, 2500));

function removePreload() {
    let cover = document.querySelector('.page-preloader-cover');
    if (!isEmpty(cover)) {
        cover.outerHTML = '';
    }
}
